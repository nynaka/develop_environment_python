pages:
  stage: deploy
  script:
    - pip3 install -r requirements.txt
    - make html
    - mkdir -p public/
    - cp -r build/html/* public/
  artifacts:
    paths:
      - public
  only:
    - main

